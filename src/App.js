"use strict";

import React from "react";

export const App = React.createClass({
    render () {
        return (
            <div>
                <h1>Wow!</h1>
            </div>
        );
    }
});
